export const dark1 = (isDarkMode?: boolean) => (isDarkMode ? '#111' : '#EEE')

export const dark2 = (isDarkMode?: boolean) =>
  isDarkMode ? 'rgb(39, 39, 42)' : 'rgb(200, 200, 200)'

export const dark3 = (isDarkMode?: boolean) => (isDarkMode ? '#333' : '#DDD')

export const dark4 = (isDarkMode?: boolean) => (isDarkMode ? '#DDD' : '#333')

export const overlayBg = (isDarkMode?: boolean) =>
  isDarkMode ? 'rgba(0, 0, 0, 0.7)' : 'rgba(200, 200, 200, 0.7)'
